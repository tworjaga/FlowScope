# 📦 FlowScope - Руководство по Установке Зависимостей

## Проблема: ModuleNotFoundError: No module named 'PyQt6'

Это руководство поможет установить все необходимые зависимости для FlowScope.

---

## ✅ Решение 1: Установка Критических Зависимостей (Рекомендуется)

### Шаг 1: Установите минимальный набор для запуска

```bash
cd flowscope
pip install --user --no-cache-dir PyQt6 pyqtgraph sqlalchemy pandas matplotlib
```

**Что устанавливается:**
- `PyQt6` - GUI framework (критически важно!)
- `pyqtgraph` - Real-time графики
- `sqlalchemy` - База данных
- `pandas` - Анализ данных
- `matplotlib` - Графики и диаграммы

**Время установки:** ~5-10 минут

---

## ✅ Решение 2: Полная Установка Всех Зависимостей

### Если у вас есть права администратора:

```bash
cd flowscope
pip install -r requirements.txt
```

### Если нет прав администратора:

```bash
cd flowscope
pip install --user -r requirements.txt
```

### Если есть проблемы с кэшем:

```bash
cd flowscope
pip install --user --no-cache-dir -r requirements.txt
```

---

## 🔧 Решение 3: Установка по Группам

Если полная установка не работает, установите зависимости группами:

### Группа 1: GUI (Критически важно!)
```bash
pip install --user --no-cache-dir PyQt6 pyqtgraph
```

### Группа 2: Анализ Данных
```bash
pip install --user --no-cache-dir numpy pandas matplotlib seaborn
```

### Группа 3: База Данных
```bash
pip install --user --no-cache-dir sqlalchemy alembic
```

### Группа 4: Экспорт
```bash
pip install --user --no-cache-dir reportlab openpyxl jinja2
```

### Группа 5: API (Опционально)
```bash
pip install --user --no-cache-dir fastapi uvicorn pydantic
```

### Группа 6: Сеть
```bash
pip install --user --no-cache-dir dnspython psutil
```

### Группа 7: Безопасность
```bash
pip install --user --no-cache-dir cryptography pyopenssl
```

---

## 🚨 Частые Проблемы и Решения

### Проблема 1: Permission Denied

**Ошибка:**
```
ERROR: Could not install packages due to an OSError: [Errno 13] Permission denied
```

**Решение:**
```bash
# Используйте --user флаг
pip install --user PyQt6

# Или запустите от имени администратора (Windows)
# ПКМ на cmd.exe → "Запуск от имени администратора"
```

### Проблема 2: Cache Error

**Ошибка:**
```
ERROR: Could not install packages due to an OSError: ... cache ...
```

**Решение:**
```bash
# Используйте --no-cache-dir флаг
pip install --user --no-cache-dir PyQt6
```

### Проблема 3: netifaces не устанавливается

**Ошибка:**
```
ERROR: Could not install packages ... netifaces ...
```

**Решение:**
```bash
# Пропустите netifaces, он опционален
# Установите остальные пакеты вручную
pip install --user --no-cache-dir PyQt6 pyqtgraph sqlalchemy pandas matplotlib
```

### Проблема 4: Долгая установка

**Причина:** PyQt6 большой пакет (~100 MB)

**Решение:** Подождите 5-10 минут, установка идет нормально

---

## ✅ Проверка Установки

После установки проверьте, что PyQt6 установлен:

```bash
python -c "import PyQt6; print('PyQt6 установлен успешно!')"
```

**Ожидаемый результат:**
```
PyQt6 установлен успешно!
```

---

## 🎯 Минимальные Требования для Запуска

Для базового запуска FlowScope нужны только:

1. ✅ **scapy** (уже установлен)
2. ✅ **PyQt6** (нужно установить)
3. ✅ **pyqtgraph** (нужно установить)
4. ✅ **sqlalchemy** (нужно установить)
5. ✅ **numpy** (обычно уже есть)

**Команда для минимальной установки:**
```bash
pip install --user --no-cache-dir PyQt6 pyqtgraph sqlalchemy
```

---

## 📋 Полный Список Зависимостей

### Критические (Обязательные):
- scapy >= 2.5.0
- PyQt6 >= 6.6.0
- pyqtgraph >= 0.13.3
- numpy >= 1.24.0
- sqlalchemy >= 2.0.0

### Важные (Рекомендуемые):
- pandas >= 2.0.0
- matplotlib >= 3.7.0
- jinja2 >= 3.1.2
- pyyaml >= 6.0.1

### Опциональные (Для дополнительных функций):
- fastapi >= 0.104.0 (REST API)
- uvicorn >= 0.24.0 (API server)
- reportlab >= 4.0.0 (PDF reports)
- openpyxl >= 3.1.0 (Excel export)
- seaborn >= 0.12.0 (Advanced charts)
- plotly >= 5.17.0 (Interactive charts)
- cryptography >= 41.0.0 (TLS analysis)
- dnspython >= 2.4.0 (DNS analysis)
- psutil >= 5.9.0 (System monitoring)

---

## 🚀 После Установки

### 1. Проверьте установку:
```bash
python -c "import PyQt6, pyqtgraph, sqlalchemy; print('Все установлено!')"
```

### 2. Запустите диагностику:
```bash
cd flowscope
python test_capture.py
```

### 3. Запустите FlowScope:
```bash
python main.py
```

Или:
```bash
start.bat
```

---

## 💡 Советы

### Совет 1: Используйте Virtual Environment (Рекомендуется)

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте (Windows)
venv\Scripts\activate

# Активируйте (Linux/Mac)
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### Совет 2: Обновите pip

```bash
python -m pip install --upgrade pip
```

### Совет 3: Проверьте версию Python

```bash
python --version
```

**Требуется:** Python 3.10 или выше

---

## 📞 Нужна Помощь?

Если проблемы продолжаются:

1. **Проверьте логи:** `logs/analyzer.log`
2. **Запустите диагностику:** `python test_capture.py`
3. **Проверьте Python версию:** `python --version` (нужен 3.10+)
4. **Проверьте pip:** `pip --version`

---

## ✅ Готово!

После установки зависимостей FlowScope готов к использованию!

```bash
cd flowscope
python main.py
```

**Наслаждайтесь профессиональным сетевым анализом! 🔍🌐**
