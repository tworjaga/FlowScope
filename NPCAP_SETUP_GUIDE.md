# 🔥 КРИТИЧЕСКИ ВАЖНО: Настройка Npcap для захвата пакетов

## Проблема: 0 пакетов при захвате

Если вы видите "0 packets captured" после 5+ минут работы, проблема в **99% случаев** связана с:
1. **Неправильной установкой Npcap**
2. **Выбором неактивного интерфейса**

---

## ✅ ШАГ 1: Правильная установка Npcap (ОБЯЗАТЕЛЬНО!)

### Удаление старого Npcap

1. **Откройте "Установка и удаление программ"** (Win + R → `appwiz.cpl`)
2. **Найдите "Npcap"** в списке
3. **Удалите полностью**
4. **Перезагрузите компьютер**

### Установка нового Npcap

1. **Скачайте последнюю версию:**
   - Официальный сайт: https://npcap.com/#download
   - Или: https://nmap.org/npcap/

2. **Запустите установщик ОТ ИМЕНИ АДМИНИСТРАТОРА**

3. **ОБЯЗАТЕЛЬНО поставьте эти галочки:**
   ```
   ☑ Install Npcap in WinPcap API-compatible Mode
   ☑ Support raw 802.11 traffic (and monitor mode)
   ☑ Install Npcap Loopback Adapter (опционально, но рекомендуется)
   ```

4. **⚠️ БЕЗ "WinPcap API-compatible Mode" Scapy будет слепым!**

5. **Перезагрузите компьютер** (да, это обязательно!)

---

## ✅ ШАГ 2: Проверка установки Npcap

### Проверка через командную строку:

```cmd
# Откройте CMD от имени администратора
cd "C:\Program Files\Npcap"
dir

# Должны быть файлы:
# - wpcap.dll
# - Packet.dll
# - NPFInstall.exe
```

### Проверка через Python:

```python
from scapy.all import get_if_list, conf

# Должен показать список интерфейсов
print("Доступные интерфейсы:")
for iface in get_if_list():
    print(f"  - {iface}")

# Проверка текущего интерфейса
print(f"\nТекущий интерфейс: {conf.iface}")
```

---

## ✅ ШАГ 3: Выбор правильного интерфейса

### Проблема: Автовыбор часто ошибается!

Windows может иметь множество интерфейсов:
- ✅ **Wi-Fi** (активный) ← НУЖЕН ЭТОТ!
- ❌ Ethernet (неактивный)
- ❌ VirtualBox Host-Only
- ❌ Hyper-V Virtual Ethernet
- ❌ WSL
- ❌ VPN адаптеры
- ❌ Loopback

### Решение 1: Использовать GUI селектор

В приложении есть **выпадающий список интерфейсов** в toolbar:
1. Запустите приложение
2. В toolbar найдите "Interface:"
3. Выберите интерфейс с иконкой **📡** (Wi-Fi)
4. Убедитесь, что показан IP адрес (не 0.0.0.0)
5. Нажмите "▶ Start"

### Решение 2: Настроить в settings.yaml

Откройте `config/settings.yaml` и измените:

```yaml
capture:
  default_interface: "Wi-Fi"  # Или имя вашего активного интерфейса
  buffer_size: 65536
  promiscuous_mode: true
```

**Как узнать имя интерфейса:**

```cmd
# Windows CMD
ipconfig

# Найдите активный адаптер с IP адресом
# Например: "Беспроводная сеть (Wi-Fi)"
```

### Решение 3: Тест с конкретным интерфейсом

Создайте тестовый скрипт `test_capture.py`:

```python
from scapy.all import sniff, get_if_list, get_if_addr

# Показать все интерфейсы
print("Доступные интерфейсы:")
for iface in get_if_list():
    try:
        ip = get_if_addr(iface)
        print(f"  {iface}")
        print(f"    IP: {ip}")
    except:
        print(f"  {iface} (нет IP)")

# Выберите интерфейс с вашим IP
INTERFACE = "Wi-Fi"  # Замените на ваш

print(f"\nЗахват на {INTERFACE}...")
print("Откройте браузер и зайдите на любой сайт")

def packet_callback(pkt):
    print(f"✓ Пакет: {pkt.summary()}")

# Тест захвата (10 пакетов)
sniff(
    iface=INTERFACE,
    filter="tcp or udp",
    prn=packet_callback,
    count=10,
    timeout=30
)
```

Запустите:
```cmd
python test_capture.py
```

**Если видите пакеты** → интерфейс правильный!
**Если 0 пакетов** → попробуйте другой интерфейс из списка.

---

## ✅ ШАГ 4: Проверка start.bat

Убедитесь, что `start.bat` содержит:

```batch
@echo off
cd /d "%~dp0"
echo Starting Network Analyzer Pro...
echo.
echo Running from: %CD%
echo.
python main.py
pause
```

**Важно:**
- `cd /d "%~dp0"` - переход в директорию скрипта
- `python main.py` - запуск правильного Python

---

## ✅ ШАГ 5: Права администратора

### Windows требует админ-прав для захвата пакетов!

**Способ 1: Через start.bat**
1. Правый клик на `start.bat`
2. "Запуск от имени администратора"

**Способ 2: Через CMD**
```cmd
# Откройте CMD от имени администратора
cd d:\pythonprojects\network-analyzer-pro
python main.py
```

**Способ 3: Постоянные права**
1. Правый клик на `python.exe`
2. Свойства → Совместимость
3. ☑ "Выполнять эту программу от имени администратора"

---

## 🔍 Диагностика проблем

### Проблема: "Permission denied" или "Access denied"
**Решение:** Запустите от имени администратора

### Проблема: "No such device exists"
**Решение:** 
1. Переустановите Npcap с WinPcap-compatible mode
2. Перезагрузите компьютер
3. Проверьте имя интерфейса

### Проблема: Интерфейс есть, но 0 пакетов
**Решение:**
1. Проверьте, что выбран АКТИВНЫЙ интерфейс (с IP)
2. Убедитесь, что через этот интерфейс идёт трафик
3. Попробуйте отключить антивирус/файрвол временно
4. Проверьте, что Npcap установлен с WinPcap mode

### Проблема: "Loopback" в списке, но нет Wi-Fi
**Решение:**
1. Npcap установлен неправильно
2. Переустановите с галочкой "Support raw 802.11 traffic"
3. Перезагрузите

---

## 📊 Проверка работоспособности

После настройки:

1. **Запустите приложение от имени администратора**
2. **Выберите Wi-Fi интерфейс** в dropdown
3. **Нажмите "▶ Start"**
4. **Откройте браузер** и зайдите на любой сайт
5. **Через 2-3 секунды** должны появиться пакеты

**Ожидаемый результат:**
```
Status: Capturing...
Packets: 15, 32, 58, 124... (растёт!)
Rate: 25 pps
```

**Если всё ещё 0 пакетов:**
- Проверьте логи: `logs/analyzer.log`
- Запустите `test_capture.py` для диагностики
- Убедитесь, что Npcap установлен правильно

---

## 🎯 Быстрый чеклист

- [ ] Npcap установлен с WinPcap API-compatible Mode
- [ ] Компьютер перезагружен после установки Npcap
- [ ] Приложение запущено от имени администратора
- [ ] Выбран активный Wi-Fi интерфейс (не Ethernet, не Loopback)
- [ ] Интерфейс имеет реальный IP адрес (не 0.0.0.0)
- [ ] Через выбранный интерфейс идёт интернет-трафик
- [ ] Антивирус не блокирует захват пакетов

---

## 💡 Дополнительные советы

### Для максимальной производительности:

1. **Используйте Wi-Fi интерфейс** (не Ethernet через репитер)
2. **Отключите ненужные виртуальные адаптеры** (VirtualBox, Hyper-V)
3. **Используйте фильтры** для уменьшения нагрузки
4. **Закройте другие программы**, использующие сеть

### Для отладки:

1. **Смотрите логи:** `logs/analyzer.log`
2. **Используйте Wireshark** для сравнения (если он работает, то и наше приложение должно)
3. **Проверяйте через `test_capture.py`**

---

## 📞 Если ничего не помогло

1. Проверьте, работает ли **Wireshark** на вашем компьютере
2. Если Wireshark работает, скопируйте его настройки Npcap
3. Убедитесь, что Python и Scapy установлены правильно:
   ```cmd
   pip install --upgrade scapy
   ```

---

**Удачи с захватом пакетов! 🚀**
